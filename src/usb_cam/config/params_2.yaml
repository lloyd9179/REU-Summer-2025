/**:
    ros__parameters:
      video_device: "/dev/video1003"   #changed from video1003 to video4,to address the "/dev/video4" in the 2nd output (search for video4 and video1003 in the output to see)
      framerate: 15.0
      io_method: "mmap"
      frame_id: "camera2"
      pixel_format: "yuyv"
      av_device_format: "YUV422P"
      image_width: 1280
      image_height: 720
      camera_name: "kiyo_pro"
      # reusing same camera intrinsics only for demo, should really be unique for camera2"
      camera_info_url: "package://usb_cam/config/camera_info_kiyo.yaml"
      brightness: -1
      contrast: -1
      saturation: -1
      sharpness: -1
      gain: -1
      auto_white_balance: true
      white_balance: 4000
      autoexposure: true
      exposure: 100
      autofocus: false
      focus: -1
